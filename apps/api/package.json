{
    "name": "api",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "dev": "if [ ! -f vendor/bin/sail ]; then composer install && php artisan sail:install; fi && ./vendor/bin/sail up -d"
    }
  }